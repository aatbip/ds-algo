#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
  double medianSortedArrays(vector<int> &nums1, vector<int> &nums2) { return 0; }
};

int main(void) {
  Solution s;
  vector<int> nums1 = {1, 3, 8};
  vector<int> nums2 = {7, 9, 10, 11};

  cout << s.medianSortedArrays(nums1, nums2) << "\n";

  return 0;
}
