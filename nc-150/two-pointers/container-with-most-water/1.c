#include <math.h>

int min(int a, int b) { return a < b ? a : b; }

int water_content(int *heights, int n) {
  int l = 0, r = n - 1;
  while (l <= r) {
  }
}

int main(void) {
  int heights[] = {1, 7, 2, 5, 4, 7, 3, 6};

  return 0;
}
