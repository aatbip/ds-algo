#include <stack>
#include <string>

using namespace std;

class Solution {
public:
  bool is_valid_parenthesis(string &s) {
    stack<char> st;
    int half = s.size() / 2;
  }
};

int main(void) {
  Solution s;

  return 0;
}
